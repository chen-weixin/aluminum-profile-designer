# 安全说明

本项目第一版计划为纯前端 PWA，默认不上传项目数据，不需要账号，不连接数据库。

## 数据边界

- 项目数据默认保存在用户浏览器本地。
- JSON/CSV 导出由用户主动触发。
- 部署静态站点时不应收集用户设计数据。

## 报告安全问题

如果发现安全问题，请在 GitHub Issue 中避免公开敏感细节。可以先开一个简短 issue，说明影响范围，再通过项目维护者指定的方式补充细节。
